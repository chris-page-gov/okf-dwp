# Local beginner journey observation

The documentation at `ae3f313b1c146f786925760a401b270a063428cc` builds
186 source pages into 414 files. The new journey follows the learning path to
the source-led manual guide, measured results and demonstration script, then
uses browser Back. It checks the immutable machine-guide link, accessible
comparison table, mobile width, explicit limits and evidence-inspection steps.

The successful observation passes eight checks across desktop and mobile
viewports, with no console errors, warnings or external requests. Five fixed
local files are served to the installed Chrome browser. Screenshots and the
exact executed harness are retained in `attempt-02/`.

`attempt-01/` is a retained evaluator failure: the page presented the correct
40/40 table but the harness incorrectly required the prose phrase “all 40”.
The corrected observation checks the accessible comparison table instead.
Before browser execution, one build invocation rejected a short commit ID and
one invocation found a missing parent receipt directory; neither produced a
browser observation. The successful build uses the full immutable identity.

This is local candidate acceptance, not a public deployment check, full
accessibility audit or evidence of improved AI answers. Later documentation
changes require a fresh publication observation.
