# Earlier local measurement: harness warning

This retained observation is not used as the final measurement. The initial coverage-loop fetcher received an options object as its latency parameter; Node reported TimeoutNaNWarning and applied an unintended one-millisecond delay. The separate explicit ten-millisecond scheduling loop was unaffected.

The exact initial harness was not retained before the numeric guard was corrected. Its report binds the engine and input digests, but not the runner. This is a preservation limitation, not a replayable performance claim. The final sibling harness is separately retained, rerun and hash-bound. No raw model reasoning or private correspondence is included.
