# Initial logical-context integration failure

The first local candidate resolved Pension Credit and abroad and fetched the authored units, but retained none of its six required paths at 512 KiB. The context correctly reported authority mismatches: the new model-derived assertions incorrectly used the generic publication class `editorial` instead of the context contract’s `model-assisted`. These edges were ineligible for required-path priority. The producer was corrected; the engine’s governance rules were preserved.

This failed candidate is retained as an integration observation, not a published service or a source-content defect. Its source manifest binding remains in the package. SHA-256 of the decompressed package: `eea9d97c5999c58ece5f151df0ec331805497a063e62d6010c7ad679a0070ae6`. No model or network call occurred.
