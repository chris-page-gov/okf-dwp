# Superseded local browser observations

These original observations belong to `dwp-combined-80a1236a793a4828ae6e`. They preceded the additive metadata rebind that removed opaque official effect identifiers from public projections after a secret-scanner false positive. Source page text, concept meanings and substantive evidence were unchanged.

The original descriptor, build receipt, packages, screenshots and browser artefact hashes are retained here. The original readme is kept as historical text; its relative links refer to the layout used at the time. These are local observations, not a public-deployment attestation. They must not be treated as checks of the replacement descriptor.

The archived build receipt names historical input and output hashes. It does not assert that those old bytes can be reconstructed from the current checkout; the entire superseded generated corpus is not duplicated here. Use the current verification directory for checks against current publication bytes.
