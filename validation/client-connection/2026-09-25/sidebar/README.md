# Edge sidebar page-tool observation — 25 September 2026

This [public-safe observation](observation.json) records two attempts with the
public Explorer Evidence workbench. The first stopped before a prompt or model
call when the native interface appeared blank or unavailable and clipboard or
window errors interrupted interaction. No cause was established.

In one later fresh conversation, the sidebar displayed **5.6 Sol Medium** and
used the existing, user-authorised CDP connection to call the page's native
`okf_get_state`, `okf_get_view_data` and `okf_show_view` tools. It showed the
staff-016 Requirements view after an initially bounded data response was
retried at 32,768 bytes. Independent page inspection saw the view change from
Calculation stages to Requirements and rows 1–3 of six. The explanation kept
`insufficient` status, noted that no source references were supplied in that
result and calculated no award.

This is a **UI and tool-trace observation**. It is not a transcript, complete
tool-response archive, independent manifest/package hash check or specialist
assessment. The result ID and revision are session-local. The sidebar had no
dedicated `webmcp` capability in this check; the observed CDP route does not
establish automatic discovery, universal host support, Voice or room audio.
No new permissions or browser settings were introduced. The user's other
sidebar draft was untouched.

The [connection guide](../../../../docs/chatgpt-connection.md) separates this
saved-package page control from fresh Reader assembly and the independently
published remote MCP service. The [sidebar guide](../../../../docs/workbench-sidebar-demo.md)
retains the practical prompt and limits.
