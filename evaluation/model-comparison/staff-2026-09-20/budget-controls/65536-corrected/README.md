# Corrected 64 KiB control

These five packages were frozen before provider calls using the corrected generic byte-budget engine. The four real questions retain 4, 6, 11 and 9 evidence records respectively, but no relationships. All remain insufficient. They are retained as budget controls; the paired claim-level trials use the same 262,144-byte package for both providers to preserve useful relationship paths.
