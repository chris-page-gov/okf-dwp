# Temporary 256 KiB sizing observation

These four substantive packages had 47–52 selected records before a generic engine budget defect was corrected. No provider calls used these packages. They are retained as a pre-call sizing observation, not a model trial. The final trial returns to the originally requested 64 KiB budget using the corrected shared engine.
