# Preregistered 64 KiB context-budget control

Before any provider calls, all four substantive cases returned a fail-closed metadata-budget refusal with zero selected records. The unknown control also had zero records, as expected. These exact earlier packages and protocol are retained. Their manifest paths describe the original export location; the copied context bytes here retain their original hashes. This is an observed negative budget control, not a model answer trial.
