# Fifth context comparison: declared source-selection paths retained

This independent review reads the retained [third](../../runs/attempt-03/report.json), [fourth](../../runs/attempt-04/report.json) and [fifth](../../runs/attempt-05/report.json) trials. It makes no new retrieval, assembly, acquisition or model calls. The protocol, evaluator and source manifests are unchanged; the fifth trial changes the admitted engine to `8a5b8d11a2d99935efca4ba8366812844061d928`.

## What passes

The corrected engine prioritises a declared path only from an actually resolved concept, after observing each exact directed, permitted relationship in the reached graph. Requirements do not supply hidden retrieval seeds. A source unit with an unresolved boundary may receive allocation priority, while its warning and insufficient status remain.

The following results use the same 40 staff questions at 512 KiB. Paths and routes count occurrences across questions, not distinct legal rules.

| Measure | Third trial | Fourth trial | Fifth trial |
| --- | ---: | ---: | ---: |
| Cases with source evidence | 39/40 | 40/40 | 40/40 |
| Inherited source-selection paths | 47/47 | 46/47 | 47/47 |
| Additional source-read selection paths | 139/181 | 137/181 | 181/181 |
| Restored location routes | 567/765 | 488/765 | 490/765 |
| Inferred profile-to-candidate routes | 39/87 | 45/87 | 35/87 |
| Original obligations closed | 0/203 | 0/203 | 0/203 |

The formerly empty `staff-009` package now contains 25 complete retained source units. The calculation question `staff-010` retains all 27 declared source-selection paths, `staff-021` retains all 11, and `staff-029` again retains all seven inherited paths, including the P5046–P5052 no-claim transition passage.

The [independent comparison receipt](independent-comparison-review.json) confirms that all 82 logical-corpus packages are byte-identical after decompression. It also checks 2,723 selected traversal paths and all 354 explicit relationship-row omissions. Every checked selected path has its exact directed assertions and selected endpoints, with declared guards satisfied. [No applicable required assertion was removed](required-edge-protection-review.json), including checks against the bound base declarations when a final package refused metadata.

**This passes the bounded source-selection allocation gate.** It does not establish legal answerability, independent relevance or model answer quality. The old failed attempts remain part of the evidence.

## Limits and trade-offs

More declared source-selection paths fit because allocation gives those observed paths priority. Some overlap-only location routes consequently fit less often: the inferred-route total falls to 35/87, and restored navigation remains below the third trial. This is an explicit trade-off, not a claim that every measure improved. The location routes never satisfy the original page-level obligations.

All 164 results remain insufficient. At 32 KiB the structured packages still retain no source evidence for any of the 40 staff questions. Retrieval and package truncation remain explicit. Specialist review, legal versions, current applicability, additional qualifications and unresolved dependencies are not supplied by better allocation.

The original imprisonment acceptance question and hospital evidence-gap control are separate from these 40 staff questions. Their equivalent semantic coverage must be checked separately; this result does not silently accept either case.

## Review method note

Consumed shard sets differ between engines because traversal order changes which shards are read. The independent check therefore compares the exact common input identities and unchanged source manifest bindings, rather than incorrectly requiring identical consumed-file lists. It verifies archived package hashes and bytes directly. It does not replay the assemblies or override their retained failures.
