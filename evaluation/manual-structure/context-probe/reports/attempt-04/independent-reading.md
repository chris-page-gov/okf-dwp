# Fourth context comparison: evidence restored, path regression retained

This independent review reads the existing [third](../../runs/attempt-03/report.json) and [fourth](../../runs/attempt-04/report.json) trial reports and exact archived packages. It makes no new retrieval, assembly or model calls. The protocol, source bindings and evaluator are unchanged; the fourth trial changes only the admitted engine identity.

## What the corrected engine changed

The engine first admits one highest-ranked, complete lexical source unit before following actually resolved concept paths. Under package-byte pressure it can omit unused full relationship rows while preserving their exact identifiers and source-bound metadata. It protects selected paths and applicable declared required paths from that relationship-row pruning.

The following are staff-case results at 512 KiB. Paths and routes count occurrences across questions, not distinct rules.

| Measure | Third trial | Fourth trial |
| --- | ---: | ---: |
| Cases with source evidence | 39/40 | 40/40 |
| Cases with relationships | 40/40 | 40/40 |
| Inherited source-selection paths | 47/47 | 46/47 |
| New source-read selection paths | 139/181 | 137/181 |
| Restored location routes | 567/765 | 488/765 |
| Inferred profile-to-candidate routes | 39/87 | 45/87 |
| Original obligations closed | 0/203 | 0/203 |

The earlier zero-evidence case `staff-009` now receives 21 complete source units, and `staff-021` increases from one to 20. All 82 logical-corpus control packages, including both budgets and unknown controls, are byte-identical after decompression. Neither the legacy corpus nor its delivery behaviour changed.

However, the fourth trial is **not accepted as a replacement**: `staff-029`, concerning the relationship between IIDB, PIP, Attendance Allowance and DLA, loses one inherited source-selection path. Its P5046–P5052 no-claim transition unit is explicitly listed in the final byte-budget omissions. That case falls from seven retained inherited paths to six. The new calculation/eligibility selection for `staff-010` also falls from 19 paths to four out of 27. These losses are retained, not hidden by the improved source-record count.

At 32 KiB the structured corpus still returns no source evidence in any staff case. All staff results remain insufficient at both budgets, with retrieval and assembly truncation explicit. More retained passages do not close legal-version, applicability or specialist-review obligations.

## Independent integrity checks

The [comparison review](independent-comparison-review.json) verifies the unchanged legacy package bytes and 2,685 retained selected paths. Every checked path retains its selected endpoints and exact directed assertions.

The [required-edge check](required-edge-protection-review.json) checks all 359 explicit relationship-row omissions against the bound base index, including cases where final metadata was refused. None removes an applicable required assertion. The missing P5046–P5052 source record is therefore an allocation loss under the source-byte budget, not evidence that the relationship-pruning protection silently deleted its declared required edge.

The next correction must preserve the fixed source and improve generic allocation. A new attempt should retain this failure, restore all 47 inherited paths, check the broader source-read selections and confirm legacy replay remains unchanged. No source/profile editing or larger resource ceiling is justified merely to make this test pass.
