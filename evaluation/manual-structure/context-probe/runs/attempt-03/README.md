# Source and semantic increment: retained allocation failure

This trial uses the final 53,727-unit source projection and the independently
reviewed guarded engine `c19912ba2f70d739c732d0c04842c6c2c6b3f333`. It runs all
40 staff occurrences plus one unknown-term control, against two projections and
two budgets: **164 assemblies**, no network or model calls.

The [report](report.json) retains exact sources, engine, metrics helper, runner
and context bytes. It separates the seven inherited logical requirements, seven
additional source-read requirements and 40 unchanged legacy page requirements.
Legacy location navigation has its own denominator and cannot close the 203
original obligations.

**Adoption is held.** At 512 KiB all 47 inherited paths remain, but Staff 009
returns no evidence, a regression from the preceding trial. New source-read
paths retain 139/181 occurrences. The 64-file retrieval limit is reached;
adjacency expansion can consume capacity before complete source reads. Keep
this failure and evaluate a generic allocation repair on these same inputs.
Do not increase limits or treat missing evidence as a complete answer.

At 32 KiB all 40 structured results still contain no source evidence. Every
result at both budgets remains insufficient. Neither path counts nor location
overlap measure independent relevance, model-answer accuracy or specialist
acceptance.
