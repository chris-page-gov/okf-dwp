[project]
name = "okf-dwp-liteparse-pilot"
version = "0.1.0"
description = "Isolated, source-bound LiteParse comparison; no production dependency"
requires-python = ">=3.12.11,<3.13"
dependencies = ["liteparse==2.14.6"]

[tool.uv]
package = false
